# =============================================================================
#  Interfaz web (Streamlit).
#
#  Imagen separada e intencionadamente ligera: la UI solo habla HTTP con la
#  API, así que no necesita ni navegador, ni modelos ONNX, ni driver de base de
#  datos. Separarlas permite además escalar UI y backend por su cuenta.
# =============================================================================
FROM python:3.12-slim-bookworm

ENV PYTHONUNBUFFERED=1 \
    PYTHONDONTWRITEBYTECODE=1 \
    PIP_NO_CACHE_DIR=1

RUN pip install --no-cache-dir \
        "streamlit>=1.40" \
        "pandas>=2.2" \
        "requests>=2.32"

RUN useradd --create-home --shell /bin/bash app
WORKDIR /app
COPY --chown=app:app src/rag_assistant/ui/streamlit_app.py ./streamlit_app.py
USER app

EXPOSE 8501

HEALTHCHECK --interval=30s --timeout=5s --start-period=20s --retries=3 \
    CMD python -c "import urllib.request,sys; \
        sys.exit(0 if urllib.request.urlopen('http://localhost:8501/_stcore/health', timeout=5).status == 200 else 1)"

CMD ["streamlit", "run", "streamlit_app.py", \
     "--server.port=8501", \
     "--server.address=0.0.0.0", \
     "--server.headless=true", \
     "--browser.gatherUsageStats=false"]
